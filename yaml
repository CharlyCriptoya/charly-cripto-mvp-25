services:
  - type: web
    name: charly-cripto
    env: node
    rootDir: .
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
    plan: free
